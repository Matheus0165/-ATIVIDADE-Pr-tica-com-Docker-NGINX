version: '3.8'

services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./sites/blog:/var/www/blog:ro
      - ./sites/docs:/var/www/docs:ro
